---
phase: quick
plan: 260327-tv6
type: execute
wave: 1
depends_on: []
files_modified:
  - aegisgate/policies/rules/default.yaml
  - aegisgate/policies/rules/strict.yaml
  - aegisgate/adapters/openai_compat/upstream.py
  - aegisgate/config/settings.py
  - config/README.md
  - README.md
  - README_zh.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "default.yaml declared filters match actual runtime defaults (no dead entries)"
    - "README documents error response JSON format and custom HTTP headers"
    - "config/README.md explains feature flag / policy YAML / security_level interaction"
    - "strict.yaml has design-intent comment explaining why it has fewer filters but lower threshold"
    - "upstream.py timeout comment is accurate"
    - "settings.py deprecation annotation is clear"
  artifacts:
    - path: "aegisgate/policies/rules/default.yaml"
      provides: "Accurate default policy without dead filter entries"
    - path: "README.md"
      provides: "Error response format + HTTP headers reference sections"
    - path: "README_zh.md"
      provides: "Error response format + HTTP headers reference sections (Chinese)"
    - path: "config/README.md"
      provides: "Feature flag / policy / security_level interaction section"
  key_links: []
---

<objective>
Fix 7 documentation consistency audit findings across policy YAML, README files, config docs, and source comments.

Purpose: Align declared policy with runtime behavior, document undocumented gateway features (error format, HTTP headers, config interactions), and fix inaccurate comments.
Output: Corrected YAML, updated READMEs, updated config docs, fixed source comments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@aegisgate/policies/rules/default.yaml
@aegisgate/policies/rules/strict.yaml
@aegisgate/config/settings.py
@aegisgate/adapters/openai_compat/upstream.py
@aegisgate/core/gateway_auth.py
@config/README.md
@README.md
@README_zh.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix policy YAML and source comment inaccuracies</name>
  <files>aegisgate/policies/rules/default.yaml, aegisgate/policies/rules/strict.yaml, aegisgate/adapters/openai_compat/upstream.py, aegisgate/config/settings.py</files>
  <action>
1. **default.yaml** (Finding #1 — HIGH): Remove `system_prompt_guard` from `enabled_filters` list. The feature flag `enable_system_prompt_guard` defaults to `False` in settings.py, and `policy_engine.py` uses it as an AND condition, so system_prompt_guard never activates. Removing it from the YAML makes the declared policy match actual behavior. Do NOT change the settings.py default (changing to True could break existing deployments).

2. **strict.yaml** (Finding #5 — MEDIUM): Add a comment block at the top of the file explaining design intent:
   - strict.yaml uses a LOWER risk_threshold (0.50 vs default 0.85), meaning it triggers on lower-confidence detections
   - It intentionally omits `system_prompt_guard` and `untrusted_content_guard` because those filters are better suited to multi-tenant scenarios; the strict profile focuses on tightening the core detection pipeline threshold instead
   - The name "strict" refers to the stricter threshold, not to having more filters

3. **upstream.py** (Finding #6 — LOW): At line 48, change the comment from `# connect/write use capped values; read uses the full timeout for long-running LLM requests` to `# connect is capped at 30s; read/write/pool use the full timeout for long-running LLM requests`. The code shows only `connect = min(timeout, 30.0)` is capped — write, read, and pool all use the uncapped `timeout` value.

4. **settings.py** (Finding #7 — LOW): Enhance the deprecation comment on `require_confirmation_on_block` (around line 67-69). Change the existing 2-line comment to:
   ```
   # [DEPRECATED] require_confirmation_on_block — 此字段已废弃，保留仅为配置兼容。
   # 无论该值设为 True 或 False，行为均等同 False：拦截时直接将危险片段
   # 变形后返回结果，不再支持 yes/no 放行指令。
   # UI (.env.example, config/README.md) 已标注废弃；未来版本可能移除此字段。
   ```
  </action>
  <verify>
    <automated>cd /mnt/d/agent_work/AegisGate && python -c "import yaml; d=yaml.safe_load(open('aegisgate/policies/rules/default.yaml')); assert 'system_prompt_guard' not in d['enabled_filters'], 'system_prompt_guard still in default.yaml'" && python -c "
with open('aegisgate/adapters/openai_compat/upstream.py') as f:
    content = f.read()
assert 'connect is capped' in content, 'upstream.py comment not updated'
assert 'connect/write use capped' not in content, 'old comment still present'
" && echo "PASS"</automated>
  </verify>
  <done>default.yaml no longer lists system_prompt_guard; strict.yaml has design-intent comment; upstream.py comment is accurate; settings.py deprecation is clearly marked</done>
</task>

<task type="auto">
  <name>Task 2: Add error response format and HTTP headers to READMEs</name>
  <files>README.md, README_zh.md</files>
  <action>
1. **README.md** (Findings #2, #3): After the existing "### Security Pipeline" section (around line 258), add two new subsections:

   **### Error Response Format**
   Document the structured error JSON that the gateway returns. The gateway uses `_blocked_response()` from `gateway_auth.py` which returns:
   ```json
   {
     "error": {
       "code": "<error_code>",
       "message": "<human-readable reason>",
       "aegisgate": { ... }
     }
   }
   ```
   Note that boundary rejections (auth failures, invalid tokens, rate limits) return this nested error format. Filter pipeline results may include `aegisgate` metadata in the response body with risk scores and disposition info. Mention the common error codes: `unauthorized`, `forbidden`, `invalid_filter_mode`, `rate_limited`, `request_too_large`.

   **### Custom HTTP Headers**
   Document these request-side headers clients can send:
   - `x-aegis-filter-mode`: Set to `passthrough` to skip redaction/sanitization, or `redact` (default behavior). Also settable via token path suffix `token__passthrough` / `token__redact`.
   - `x-aegis-redaction-whitelist`: Comma-separated list of redaction keys to exempt from redaction (e.g., field names that look like secrets but are safe).
   - `x-aegis-request-id`: Injected by the gateway into upstream-bound requests for tracing correlation. Not set by clients — appears in upstream headers and gateway logs.

2. **README_zh.md** (Findings #2, #3): Add equivalent Chinese sections. Place after the "### 1.5 命中后的处理方式" section. Use the same technical content translated to Chinese, matching the existing README_zh.md style (Chinese prose with English technical terms preserved).
  </action>
  <verify>
    <automated>cd /mnt/d/agent_work/AegisGate && grep -q "Error Response Format" README.md && grep -q "Custom HTTP Headers" README.md && grep -q "x-aegis-filter-mode" README.md && grep -q "x-aegis-filter-mode" README_zh.md && echo "PASS"</automated>
  </verify>
  <done>Both READMEs document error response JSON format and all three custom HTTP headers with usage examples</done>
</task>

<task type="auto">
  <name>Task 3: Add feature flag / policy / security_level interaction to config README</name>
  <files>config/README.md</files>
  <action>
(Finding #4 — MEDIUM): After the existing "### 2. 运行参数（.env）" table section and before the "注意：" block, add a new subsection:

**### 配置交互：Feature Flag / 策略 YAML / Security Level**

Explain the three-way interaction in Chinese (matching existing config/README.md language):

1. **策略 YAML (`enabled_filters`)** declares which filters are candidates for activation.
2. **Feature flags (`enable_*` in settings.py / .env)** are AND conditions — a filter must be listed in YAML AND have its feature flag enabled to activate. Example: `system_prompt_guard` is in default.yaml but `enable_system_prompt_guard=False` by default, so it never activates unless the operator explicitly enables the flag.
3. **`AEGIS_SECURITY_LEVEL`** does NOT change which filters run. It adjusts `risk_threshold` via the policy engine:
   - `low`: higher threshold (more permissive, fewer blocks)
   - `medium` (default): uses the YAML-declared `risk_threshold`
   - `high`: lower threshold (more aggressive, more blocks)

Include a concrete example: "To enable system_prompt_guard, you must BOTH keep it in the policy YAML AND set `AEGIS_ENABLE_SYSTEM_PROMPT_GUARD=true` in `.env`."

Add a brief note: "热更新限制：`security_level` 变更不会在热更新时生效，需重启。"
  </action>
  <verify>
    <automated>cd /mnt/d/agent_work/AegisGate && grep -q "Feature Flag" config/README.md && grep -q "AND" config/README.md && grep -q "security_level" config/README.md && echo "PASS"</automated>
  </verify>
  <done>config/README.md explains the three-way interaction between feature flags, policy YAML, and security_level with concrete examples</done>
</task>

</tasks>

<verification>
All 7 findings addressed:
- Finding #1 (HIGH): default.yaml system_prompt_guard removed
- Finding #2 (HIGH): Error response format documented in both READMEs
- Finding #3 (MEDIUM): HTTP headers documented in both READMEs
- Finding #4 (MEDIUM): Config interaction section added
- Finding #5 (MEDIUM): strict.yaml design-intent comment added
- Finding #6 (LOW): upstream.py comment corrected
- Finding #7 (LOW): settings.py deprecation annotation enhanced
</verification>

<success_criteria>
- `system_prompt_guard` not in default.yaml enabled_filters
- Both READMEs contain error response format and HTTP headers sections
- config/README.md explains feature flag / policy YAML / security_level interaction
- strict.yaml has design-intent comment at top
- upstream.py timeout comment says "connect is capped" not "connect/write use capped"
- settings.py deprecation comment mentions future removal
- All existing tests still pass: `python -m pytest aegisgate/tests/ -x -q`
</success_criteria>

<output>
After completion, create `.planning/quick/260327-tv6-fix-doc-consistency-audit-findings/260327-tv6-SUMMARY.md`
</output>
