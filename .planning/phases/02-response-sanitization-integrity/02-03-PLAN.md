---
phase: 02-response-sanitization-integrity
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - aegisgate/adapters/openai_compat/router.py
  - aegisgate/adapters/openai_compat/stream_utils.py
  - aegisgate/tests/test_streaming_router.py
autonomous: true
requirements:
  - RESP-02
  - RESP-03
must_haves:
  truths:
    - "Direct `/v1/messages` streaming sanitize-path output keeps Anthropic SSE framing instead of emitting chat chunks or plain text."
    - "Only text-bearing direct messages stream content is sanitized; unrelated event structure stays valid."
    - "Sanitized direct messages streaming still carries operator-visible risk marks through the existing `aegisgate`/audit channels."
  artifacts:
    - path: "aegisgate/adapters/openai_compat/stream_utils.py"
      provides: "Anthropic-native SSE sanitize helpers for direct `/v1/messages` output."
    - path: "aegisgate/adapters/openai_compat/router.py"
      provides: "Direct `/v1/messages` stream sanitize wiring that reuses the new Anthropic SSE helpers."
    - path: "aegisgate/tests/test_streaming_router.py"
      provides: "Direct `/v1/messages` sanitize-path streaming regressions with offload-safe monkeypatching."
  key_links:
    - from: "aegisgate/adapters/openai_compat/router.py"
      to: "aegisgate/adapters/openai_compat/stream_utils.py"
      via: "`_execute_messages_stream_once()` yielding Anthropic-native sanitized SSE events rather than `_stream_confirmation_sse_chunk()` chat payloads."
      pattern: "_execute_messages_stream_once|messages.*stream.*sanitize"
    - from: "aegisgate/tests/test_streaming_router.py"
      to: "aegisgate/adapters/openai_compat/router.py"
      via: "direct messages stream regressions asserting `message_start`/`content_block_delta` framing and no chat chunk fallback."
      pattern: "test_messages_stream_.*sanitize"
---

<objective>
Repair the direct `/v1/messages` stream sanitize path so high-risk streaming responses remain Anthropic-compatible while still redacting dangerous fragments.

Purpose: satisfy the streaming slice of `RESP-02` and `RESP-03` by replacing the current chat-style or plain-text sanitize fallback with Anthropic-native SSE output.
Output: direct messages streaming regressions plus SSE helpers and router wiring for Anthropic sanitize-path rendering.
</objective>

<execution_context>
@/home/yz/.codex/get-shit-done/workflows/execute-plan.md
@/home/yz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-response-sanitization-integrity/02-RESEARCH.md
@.planning/phases/02-response-sanitization-integrity/02-VALIDATION.md
@.planning/phases/02-response-sanitization-integrity/02-01-SUMMARY.md
@.planning/phases/02-response-sanitization-integrity/02-02-SUMMARY.md
@aegisgate/adapters/openai_compat/router.py
@aegisgate/adapters/openai_compat/stream_utils.py
@aegisgate/tests/test_streaming_router.py

<interfaces>
From `aegisgate/adapters/openai_compat/stream_utils.py`:
```python
def _stream_confirmation_sse_chunk(
    ctx: RequestContext,
    model: str,
    route: str,
    content: str,
    confirmation_meta: dict[str, Any] | None,
) -> bytes
def _stream_done_sse_chunk() -> bytes
def _extract_sse_data_payload_from_chunk(chunk: bytes) -> str | None
def _iter_sse_frames(chunks: AsyncGenerator[bytes, None]) -> AsyncGenerator[bytes, None]
```

From `aegisgate/adapters/openai_compat/router.py`:
```python
async def _execute_messages_stream_once(
    *,
    payload: dict[str, Any],
    request_headers: Mapping[str, str],
    request_path: str,
    boundary: dict | None,
    tenant_id: str = "default",
) -> StreamingResponse | JSONResponse
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace direct `/v1/messages` stream sanitize fallback with Anthropic-native SSE output</name>
  <files>aegisgate/adapters/openai_compat/router.py, aegisgate/adapters/openai_compat/stream_utils.py, aegisgate/tests/test_streaming_router.py</files>
  <read_first>
    .planning/phases/02-response-sanitization-integrity/02-RESEARCH.md
    .planning/phases/02-response-sanitization-integrity/02-VALIDATION.md
    .planning/phases/02-response-sanitization-integrity/02-01-SUMMARY.md
    .planning/phases/02-response-sanitization-integrity/02-02-SUMMARY.md
    aegisgate/adapters/openai_compat/router.py
    aegisgate/adapters/openai_compat/stream_utils.py
    aegisgate/tests/test_streaming_router.py
  </read_first>
  <behavior>
    - Direct `/v1/messages` auto-sanitize streaming emits Anthropic-native SSE framing rather than `chat.completion.chunk` payloads or free-form text.
    - Sanitization only touches text-bearing stream payloads; unknown or non-text events remain structurally valid.
    - Operator-visible risk marks remain in existing `aegisgate` metadata or audit channels and do not create a new client protocol contract.
  </behavior>
  <action>Extend `aegisgate/tests/test_streaming_router.py` with direct `/v1/messages` sanitize-path regressions that monkeypatch `_run_payload_transform`, `_run_request_pipeline`, `_run_response_pipeline`, `_build_streaming_response`, and `_forward_stream_lines` as required by validation to avoid executor timeout failures. Assert the sanitized output contains Anthropic-native events such as `message_start`, `content_block_delta`, `message_delta`, and `message_stop` (or the repository’s direct equivalent), does not emit `chat.completion.chunk`, does not leak the original dangerous fragment, and still includes the existing `aegisgate` risk metadata channel where appropriate. Then implement the smallest brownfield-safe SSE helper set in `aegisgate/adapters/openai_compat/stream_utils.py` and wire `aegisgate/adapters/openai_compat/router.py` so `_execute_messages_stream_once()` uses those helpers when auto-sanitizing instead of `_stream_confirmation_sse_chunk()` chat/responses fallback behavior. Reuse the chat/responses SSE patching discipline as the baseline, but keep direct messages event shapes Anthropic-native and leave chat/responses streaming logic untouched.</action>
  <acceptance_criteria>
    `rg -n "test_messages_stream_sanitize_preserves_anthropic_sse|test_messages_stream_auto_sanitize_does_not_emit_chat_chunks|messages.*stream.*sanitize" aegisgate/tests/test_streaming_router.py aegisgate/adapters/openai_compat/router.py aegisgate/adapters/openai_compat/stream_utils.py`
    `pytest -q aegisgate/tests/test_streaming_router.py -k "messages_stream_sanitize or messages_stream_auto_sanitize" -x`
    `pytest -q aegisgate/tests/test_streaming_router.py -k "messages_stream_harness" -x`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q aegisgate/tests/test_streaming_router.py -k "messages_stream_harness or messages_stream_sanitize or messages_stream_auto_sanitize" -x</automated>
  </verify>
  <done>Direct `/v1/messages` sanitize-path streaming uses Anthropic-compatible SSE framing with fragment-level replacement, no chat-chunk fallback, and existing metadata/audit risk marking.</done>
</task>

</tasks>

<verification>
Run the direct `/v1/messages` streaming slice and confirm dangerous fragments are replaced without breaking Anthropic SSE framing or reusing chat/responses fallback payload shapes.
</verification>

<success_criteria>
High-risk direct `/v1/messages` streaming responses stay parseable by Anthropic-compatible clients while dangerous fragments are replaced and risk marking remains operator-visible through existing channels.
</success_criteria>

<output>
After completion, create `.planning/phases/02-response-sanitization-integrity/02-03-SUMMARY.md`
</output>
