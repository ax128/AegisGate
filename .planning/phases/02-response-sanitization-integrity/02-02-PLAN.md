---
phase: 02-response-sanitization-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - aegisgate/adapters/openai_compat/router.py
  - aegisgate/tests/test_response_sanitization_routes.py
  - README.md
  - README_zh.md
autonomous: true
requirements:
  - RESP-02
  - RESP-03
  - RESP-04
must_haves:
  truths:
    - "Direct `/v1/messages` JSON sanitize-path responses remain Anthropic-compatible instead of collapsing to `sanitized_text` or plain text."
    - "Only dangerous fragments are replaced in direct `/v1/messages` JSON responses; safe content and non-text blocks remain intact."
    - "Sanitized direct `/v1/messages` JSON responses continue to expose risk marks through existing `aegisgate` metadata and audit events."
  artifacts:
    - path: "aegisgate/adapters/openai_compat/router.py"
      provides: "Anthropic-native JSON patch/render helpers for direct `/v1/messages` sanitize-path output."
    - path: "aegisgate/tests/test_response_sanitization_routes.py"
      provides: "Direct `/v1/messages` JSON sanitize-path contract regressions."
  key_links:
    - from: "aegisgate/adapters/openai_compat/router.py"
      to: "aegisgate/adapters/openai_compat/router.py"
      via: "`_execute_messages_once()` calling a messages-native sanitize renderer modeled after chat/responses helpers."
      pattern: "_execute_messages_once|_patch_messages_response_body|_render_non_confirmation_messages_output"
    - from: "aegisgate/tests/test_response_sanitization_routes.py"
      to: "aegisgate/adapters/openai_compat/router.py"
      via: "JSON sanitize-path tests asserting Anthropic `type=message` and `content[]` output plus `aegisgate` metadata."
      pattern: "test_messages_json_.*sanitize"
---

<objective>
Repair the direct `/v1/messages` non-stream sanitize path so response-side fragment replacement preserves Anthropic JSON structure and keeps risk marks out of the client protocol contract.

Purpose: satisfy the direct JSON slice of `RESP-02`, `RESP-03`, and `RESP-04` by replacing the current `{"sanitized_text": ...}` fallback with Anthropic-native patching.
Output: direct `/v1/messages` JSON sanitize-path tests plus router helpers and wiring that reuse the existing chat/responses rendering approach.
</objective>

<execution_context>
@/home/yz/.codex/get-shit-done/workflows/execute-plan.md
@/home/yz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-response-sanitization-integrity/02-RESEARCH.md
@.planning/phases/02-response-sanitization-integrity/02-VALIDATION.md
@.planning/phases/02-response-sanitization-integrity/02-01-SUMMARY.md
@aegisgate/adapters/openai_compat/router.py
@aegisgate/adapters/openai_compat/mapper.py
@aegisgate/tests/test_response_sanitization_routes.py

<interfaces>
From `aegisgate/adapters/openai_compat/router.py`:
```python
def _patch_chat_response_body(upstream_body: dict[str, Any], ctx: RequestContext) -> dict[str, Any]
def _patch_responses_body(upstream_body: dict[str, Any], ctx: RequestContext) -> dict[str, Any]
def _render_non_confirmation_chat_response(upstream_body: dict[str, Any] | str, final_resp: InternalResponse, ctx: RequestContext) -> dict[str, Any]
def _render_non_confirmation_responses_output(upstream_body: dict[str, Any] | str, final_resp: InternalResponse, ctx: RequestContext) -> dict[str, Any]
```

From `aegisgate/adapters/openai_compat/mapper.py`:
```python
def to_messages_response(resp: InternalResponse) -> dict
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace direct `/v1/messages` JSON sanitize fallback with Anthropic-native fragment patching</name>
  <files>aegisgate/adapters/openai_compat/router.py, aegisgate/tests/test_response_sanitization_routes.py</files>
  <read_first>
    .planning/phases/02-response-sanitization-integrity/02-RESEARCH.md
    .planning/phases/02-response-sanitization-integrity/02-VALIDATION.md
    .planning/phases/02-response-sanitization-integrity/02-01-SUMMARY.md
    aegisgate/adapters/openai_compat/router.py
    aegisgate/adapters/openai_compat/mapper.py
    aegisgate/tests/test_response_sanitization_routes.py
  </read_first>
  <behavior>
    - Direct `/v1/messages` sanitize-path JSON responses keep Anthropic `type`, `role`, `content`, `model`, `usage`, and other upstream fields instead of returning `sanitized_text`.
    - Dangerous fragments are replaced only inside text-bearing content blocks while safe surrounding structure and unknown non-text fields remain unchanged.
    - Sanitized direct messages JSON responses keep risk marks in the existing `aegisgate` metadata and `_write_audit_event` payloads, not new top-level protocol fields.
  </behavior>
  <action>In `aegisgate/tests/test_response_sanitization_routes.py`, add direct `/v1/messages` JSON sanitize-path regressions that cover both `ctx.response_disposition == "sanitize"` and the broader `_needs_confirmation(ctx)` auto-sanitize branch called out in research. Assert Anthropic-native output (`type: "message"`, `content[]`, `stop_reason`, preserved IDs/model/usage/unknown fields), fragment-only replacement, existing `aegisgate` metadata, and audit writes. Then update `aegisgate/adapters/openai_compat/router.py` by adding messages-native JSON patch/render helpers modeled on `_patch_chat_response_body()` and `_render_non_confirmation_responses_output()`. Wire `_execute_messages_once()` sanitize and auto-sanitize branches through those helpers after `_attach_security_metadata()` instead of returning `{"sanitized_text": ...}` or plain text for dict upstream bodies. Keep compat=`openai_chat`, generic `/v1/*`, and chat/responses branches unchanged.</action>
  <acceptance_criteria>
    `rg -n "_patch_messages_response_body|_render_non_confirmation_messages_output|test_messages_json_sanitize_preserves_anthropic_shape|test_messages_json_auto_sanitize_preserves_anthropic_shape" aegisgate/adapters/openai_compat/router.py aegisgate/tests/test_response_sanitization_routes.py`
    `pytest -q aegisgate/tests/test_response_sanitization_routes.py -k "messages_json_sanitize or messages_json_auto_sanitize" -x`
    `pytest -q aegisgate/tests/test_response_sanitization_routes.py -k "aegisgate_metadata_channel or benign_response_paths" -x`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q aegisgate/tests/test_response_sanitization_routes.py -k "messages_json_sanitize or messages_json_auto_sanitize or aegisgate_metadata_channel or benign_response_paths" -x</automated>
  </verify>
  <done>Direct `/v1/messages` non-stream sanitize responses are Anthropic-compatible, dangerous fragments are replaced in place, and risk marking remains in the existing metadata/audit channels.</done>
</task>

<task type="auto">
  <name>Task 2: Document the `/v1/messages` JSON sanitize contract</name>
  <files>README.md, README_zh.md</files>
  <read_first>
    README.md
    README_zh.md
    .planning/phases/02-response-sanitization-integrity/02-RESEARCH.md
    aegisgate/tests/test_response_sanitization_routes.py
  </read_first>
  <action>Update `README.md` and `README_zh.md` so the `/v1/messages` documentation explicitly states that sanitized non-stream JSON responses preserve Anthropic-native `type/message/content[]` structure, replace only dangerous text fragments, and keep risk marks in the existing `aegisgate` metadata and audit channels instead of introducing new client-visible protocol fields.</action>
  <acceptance_criteria>
    `rg -n 'sanitized non-stream JSON responses preserve Anthropic-native|risk marks in the existing aegisgate metadata' README.md`
    `rg -n 'Anthropic|/v1/messages|aegisgate' README_zh.md`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'sanitized non-stream JSON responses preserve Anthropic-native|risk marks in the existing aegisgate metadata' README.md</automated>
    <automated>rg -n 'Anthropic|/v1/messages|aegisgate' README_zh.md</automated>
  </verify>
  <done>The README documents the direct `/v1/messages` JSON sanitize contract so user-visible behavior changes are tracked alongside the implementation.</done>
</task>

</tasks>

<verification>
Run the direct `/v1/messages` JSON sanitize slice in the new route suite and confirm the old `sanitized_text` envelope is no longer the client-visible contract for dict upstream bodies.
</verification>

<success_criteria>
High-risk direct `/v1/messages` JSON responses return a valid Anthropic message body with fragment-level replacement and existing `aegisgate` risk metadata instead of protocol-breaking fallback content.
</success_criteria>

<output>
After completion, create `.planning/phases/02-response-sanitization-integrity/02-02-SUMMARY.md`
</output>
