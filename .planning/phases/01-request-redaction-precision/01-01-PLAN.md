---
phase: 01-request-redaction-precision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aegisgate/adapters/openai_compat/router.py
  - aegisgate/adapters/openai_compat/sanitize.py
  - aegisgate/tests/test_request_redaction_routes.py
  - aegisgate/tests/test_openai_request_mapping.py
  - aegisgate/tests/test_passthrough_filter_mode.py
  - aegisgate/tests/test_sanitize_helpers.py
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
must_haves:
  truths:
    - "/v1/chat/completions forwards redacted text even when message content is a structured list of parts."
    - "/v1/responses remains the reference request-rewrite path for equivalent content."
    - "Structured request redaction preserves upstream-valid payload shape instead of flattening multimodal content."
  artifacts:
    - path: "aegisgate/adapters/openai_compat/router.py"
      provides: "Chat upstream builder that rewrites text-bearing content parts without dropping provider fields."
    - path: "aegisgate/adapters/openai_compat/sanitize.py"
      provides: "Reusable structured-content request redaction helper aligned to the responses rewrite pattern."
    - path: "aegisgate/tests/test_request_redaction_routes.py"
      provides: "Route-level chat/responses request redaction regression coverage."
  key_links:
    - from: "aegisgate/adapters/openai_compat/router.py"
      to: "aegisgate/adapters/openai_compat/sanitize.py"
      via: "_build_chat_upstream_payload calling a structured text-part rewrite helper."
      pattern: "_build_chat_upstream_payload|_sanitize_.*chat"
    - from: "aegisgate/tests/test_request_redaction_routes.py"
      to: "aegisgate/adapters/openai_compat/router.py"
      via: "patched forwarders asserting rewritten upstream payloads."
      pattern: "test_chat_request_redaction|test_responses_request_redaction"
---

<objective>
Make `/v1/chat/completions` use the same schema-safe request redaction strategy that already works on `/v1/responses`, while locking both routes with focused regression tests.

Purpose: satisfy `SAFE-01` and preserve `SAFE-02` by reusing the existing `/v1/responses` rewrite path as the reference implementation instead of inventing a parallel redaction flow.
Output: route-level tests plus a reusable structured-content helper wired into the chat upstream payload builder.
</objective>

<execution_context>
@/home/yz/.codex/get-shit-done/workflows/execute-plan.md
@/home/yz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-request-redaction-precision/01-RESEARCH.md
@.planning/phases/01-request-redaction-precision/01-VALIDATION.md
@aegisgate/adapters/openai_compat/router.py
@aegisgate/adapters/openai_compat/sanitize.py
@aegisgate/adapters/openai_compat/mapper.py
@aegisgate/tests/test_sanitize_helpers.py
@aegisgate/tests/test_passthrough_filter_mode.py

<interfaces>
From `aegisgate/adapters/openai_compat/router.py`:
```python
def _build_chat_upstream_payload(payload: dict[str, Any], sanitized_req_messages: list) -> dict[str, Any]
def _build_responses_upstream_payload(payload: dict[str, Any], sanitized_req_messages: list, *, request_id: str = "-", session_id: str = "-", route: str = "-", whitelist_keys: set[str] | None = None) -> dict[str, Any]
```

From `aegisgate/adapters/openai_compat/mapper.py`:
```python
def to_internal_chat(payload: dict) -> InternalRequest
def to_internal_responses(payload: dict) -> InternalRequest
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add redaction regressions for chat structured content and responses parity</name>
  <files>aegisgate/tests/test_request_redaction_routes.py, aegisgate/tests/test_openai_request_mapping.py, aegisgate/tests/test_sanitize_helpers.py, aegisgate/tests/test_passthrough_filter_mode.py</files>
  <read_first>
    .planning/phases/01-request-redaction-precision/01-RESEARCH.md
    aegisgate/adapters/openai_compat/router.py
    aegisgate/adapters/openai_compat/sanitize.py
    aegisgate/adapters/openai_compat/mapper.py
    aegisgate/tests/test_sanitize_helpers.py
    aegisgate/tests/test_passthrough_filter_mode.py
  </read_first>
  <action>Create route-level tests first for `SAFE-01` and `SAFE-02`. Add a new `aegisgate/tests/test_request_redaction_routes.py` that patches `_forward_json` or route-local builders and proves `/v1/chat/completions` rewrites only text-bearing structured `content` parts while preserving non-text parts and unknown provider keys, and proves `/v1/responses` keeps its current structured rewrite behavior for equivalent sensitive input. In the same route suite, add an explicit security/routing non-regression asserting a valid redactable structured chat payload is still forwarded and does not fall into an unexpected 403 sanitize/block path. Add `aegisgate/tests/test_openai_request_mapping.py` coverage showing `to_internal_chat()` still flattens structured chat content for analysis while the forwarded payload rewrite stays schema-aware. Extend `aegisgate/tests/test_sanitize_helpers.py` with helper-level expectations for structured chat part traversal, and extend `aegisgate/tests/test_passthrough_filter_mode.py` so passthrough mode on chat still forwards the untouched structured payload. Do not add manual-only assertions or broad end-to-end network tests.</action>
  <acceptance_criteria>
    `test -f aegisgate/tests/test_request_redaction_routes.py`
    `rg -n "test_chat_request_redaction_preserves_shape|test_chat_request_redaction_does_not_return_403|test_responses_request_redaction_structured_input|test_chat_passthrough_preserves_structured_content" aegisgate/tests`
    `pytest -q aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_openai_request_mapping.py aegisgate/tests/test_sanitize_helpers.py aegisgate/tests/test_passthrough_filter_mode.py -k "chat_request_redaction or responses_request_redaction or structured_content or return_403" -x`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_openai_request_mapping.py aegisgate/tests/test_sanitize_helpers.py aegisgate/tests/test_passthrough_filter_mode.py -k "chat_request_redaction or responses_request_redaction or structured_content or return_403" -x</automated>
  </verify>
  <done>Focused regressions exist for chat structured-content redaction, responses parity, helper traversal, passthrough non-regression, and no-unexpected-403 behavior for valid redactable chat payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse the responses rewrite pattern for chat upstream payloads</name>
  <files>aegisgate/adapters/openai_compat/router.py, aegisgate/adapters/openai_compat/sanitize.py</files>
  <read_first>
    .planning/phases/01-request-redaction-precision/01-RESEARCH.md
    aegisgate/adapters/openai_compat/router.py
    aegisgate/adapters/openai_compat/sanitize.py
    aegisgate/adapters/openai_compat/mapper.py
    aegisgate/tests/test_request_redaction_routes.py
    aegisgate/tests/test_sanitize_helpers.py
  </read_first>
  <behavior>
    - Structured chat `content` lists redact only text-bearing fields while preserving image, audio, file, and unknown provider fields.
    - Existing `/v1/responses` rewrite behavior remains unchanged and stays the reference implementation.
    - Passthrough mode keeps forwarding the original payload without request-pipeline mutation.
  </behavior>
  <action>Implement the smallest brownfield-safe helper in `aegisgate/adapters/openai_compat/sanitize.py` that walks OpenAI chat `messages[].content` structured parts, sanitizes only text-bearing nodes using the same upstream redaction machinery already used by `_sanitize_responses_input_for_upstream_with_hits(...)`, and returns hit metadata for logging. Wire `_build_chat_upstream_payload()` in `aegisgate/adapters/openai_compat/router.py` to call that helper instead of preserving structured content wholesale. Keep the route handler lean, keep `_build_responses_upstream_payload()` untouched as the source-of-truth pattern, and do not change the generic executor or response-side logic in this plan.</action>
  <acceptance_criteria>
    `rg -n "_build_chat_upstream_payload|sanitize.*chat" aegisgate/adapters/openai_compat/router.py aegisgate/adapters/openai_compat/sanitize.py`
    `pytest -q aegisgate/tests/test_request_redaction_routes.py::test_chat_request_redaction_preserves_shape -x`
    `pytest -q aegisgate/tests/test_request_redaction_routes.py::test_responses_request_redaction_structured_input -x`
    `pytest -q aegisgate/tests/test_passthrough_filter_mode.py -k "structured_content" -x`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_openai_request_mapping.py aegisgate/tests/test_sanitize_helpers.py aegisgate/tests/test_passthrough_filter_mode.py -k "chat_request_redaction or responses_request_redaction or structured_content" -x</automated>
  </verify>
  <done>`/v1/chat/completions` forwards redacted structured content without schema breakage, `/v1/responses` remains valid, and passthrough behavior is unchanged.</done>
</task>

</tasks>

<verification>
Run the focused request-redaction regression slice for chat/responses plus helper coverage. Confirm valid redactable chat/responses payloads do not regress into an unexpected 403 and no task in this plan changes `/v1/messages` forwarding yet.
</verification>

<success_criteria>
Equivalent sensitive content sent through chat and responses reaches the upstream in a valid route-specific payload with text redacted and non-text structure preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-request-redaction-precision/01-01-SUMMARY.md`
</output>
