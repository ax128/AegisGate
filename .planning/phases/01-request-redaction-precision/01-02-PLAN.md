---
phase: 01-request-redaction-precision
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - aegisgate/adapters/openai_compat/router.py
  - aegisgate/adapters/openai_compat/sanitize.py
  - aegisgate/tests/test_request_redaction_routes.py
  - aegisgate/tests/test_openai_request_mapping.py
  - aegisgate/tests/test_passthrough_filter_mode.py
autonomous: true
requirements:
  - SAFE-03
must_haves:
  truths:
    - "Direct `/v1/messages` traffic redacts sensitive text before upstream forwarding instead of only analyzing it."
    - "Anthropic-compatible request shape stays valid after redaction for both JSON and streaming paths."
    - "The compat=`openai_chat` `/v1/messages` path continues to reuse the `/v1/responses` rewrite flow."
  artifacts:
    - path: "aegisgate/adapters/openai_compat/router.py"
      provides: "Dedicated messages request execution path with schema-aware upstream payload rewriting."
    - path: "aegisgate/adapters/openai_compat/sanitize.py"
      provides: "Anthropic messages/system structured rewrite helper."
    - path: "aegisgate/tests/test_request_redaction_routes.py"
      provides: "Route-level direct `/v1/messages` rewrite coverage."
  key_links:
    - from: "aegisgate/adapters/openai_compat/router.py"
      to: "aegisgate/adapters/openai_compat/sanitize.py"
      via: "messages route using a dedicated structured rewrite helper instead of `_execute_generic_once()` sanitize-block behavior."
      pattern: "_build_messages_upstream_payload|messages"
    - from: "aegisgate/adapters/openai_compat/router.py"
      to: "/v1/responses"
      via: "_messages_compat_openai_chat reusing the existing responses path."
      pattern: "_messages_compat_openai_chat|_execute_responses"
---

<objective>
Close the remaining route gap by giving direct `/v1/messages` a protocol-aware request rewrite path instead of the current analyze-only generic forwarding behavior.

Purpose: satisfy `SAFE-03` explicitly and keep the already-correct compat conversion path intact.
Output: dedicated `/v1/messages` request rewrite helper, route wiring for JSON and streaming, and focused regression tests.
</objective>

<execution_context>
@/home/yz/.codex/get-shit-done/workflows/execute-plan.md
@/home/yz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-request-redaction-precision/01-RESEARCH.md
@.planning/phases/01-request-redaction-precision/01-01-SUMMARY.md
@aegisgate/adapters/openai_compat/router.py
@aegisgate/adapters/openai_compat/sanitize.py
@aegisgate/adapters/openai_compat/mapper.py
@aegisgate/tests/test_passthrough_filter_mode.py

<interfaces>
From `aegisgate/adapters/openai_compat/mapper.py`:
```python
def to_internal_messages(payload: dict) -> InternalRequest
def messages_payload_to_responses_payload(payload: dict, *, model_map: dict[str, str] | None = None, default_model: str | None = None) -> dict
```

From `aegisgate/adapters/openai_compat/router.py`:
```python
async def messages(payload: dict, request: Request)
async def _messages_compat_openai_chat(*, payload: dict, request: Request, gateway_headers: dict[str, str], boundary: dict | None, tenant_id: str) -> JSONResponse | StreamingResponse
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing coverage for direct `/v1/messages` rewrite behavior</name>
  <files>aegisgate/tests/test_request_redaction_routes.py, aegisgate/tests/test_openai_request_mapping.py, aegisgate/tests/test_passthrough_filter_mode.py</files>
  <read_first>
    .planning/phases/01-request-redaction-precision/01-RESEARCH.md
    .planning/phases/01-request-redaction-precision/01-01-SUMMARY.md
    aegisgate/adapters/openai_compat/router.py
    aegisgate/adapters/openai_compat/mapper.py
    aegisgate/tests/test_request_redaction_routes.py
    aegisgate/tests/test_passthrough_filter_mode.py
  </read_first>
  <action>Add route-level tests that expose the current direct `/v1/messages` gap described in research: non-compat JSON forwarding, non-compat streaming forwarding, and top-level `system` plus `messages[].content` structured blocks must all reach the upstream with redacted text and preserved Anthropic shape. Add mapper-focused tests in `aegisgate/tests/test_openai_request_mapping.py` confirming analysis flattening remains separate from forwarded payload rewriting. Extend `aegisgate/tests/test_passthrough_filter_mode.py` so passthrough mode on `/v1/messages` still skips rewriting and so compat=`openai_chat` continues to delegate to the responses path rather than the new direct rewrite path.</action>
  <acceptance_criteria>
    `rg -n "test_messages_request_redaction_preserves_anthropic_shape|test_messages_stream_request_redaction_preserves_anthropic_shape|test_messages_passthrough_preserves_payload" aegisgate/tests`
    `pytest -q aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_openai_request_mapping.py aegisgate/tests/test_passthrough_filter_mode.py -k "messages_request_redaction or anthropic_shape or messages_passthrough" -x`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_openai_request_mapping.py aegisgate/tests/test_passthrough_filter_mode.py -k "messages_request_redaction or anthropic_shape or messages_passthrough" -x</automated>
  </verify>
  <done>Direct `/v1/messages` JSON/streaming rewrite expectations and passthrough/compat guards are encoded in automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace direct `/v1/messages` analyze-only forwarding with schema-aware rewrite</name>
  <files>aegisgate/adapters/openai_compat/router.py, aegisgate/adapters/openai_compat/sanitize.py</files>
  <read_first>
    .planning/phases/01-request-redaction-precision/01-RESEARCH.md
    .planning/phases/01-request-redaction-precision/01-01-SUMMARY.md
    aegisgate/adapters/openai_compat/router.py
    aegisgate/adapters/openai_compat/sanitize.py
    aegisgate/adapters/openai_compat/mapper.py
    aegisgate/tests/test_request_redaction_routes.py
  </read_first>
  <behavior>
    - Direct `/v1/messages` rewrites redacted text into top-level `system` and message content blocks before calling the upstream forwarder.
    - JSON and streaming direct-message paths share the same rewrite logic.
    - The compat=`openai_chat` path keeps delegating into the existing responses execution flow and is not reimplemented here.
  </behavior>
  <action>Add a dedicated Anthropic-messages upstream payload builder in `aegisgate/adapters/openai_compat/router.py` plus any small supporting helper in `aegisgate/adapters/openai_compat/sanitize.py` needed to sanitize top-level `system` and `messages[].content` structured blocks while preserving Anthropic-only fields such as `max_tokens`, `tool_choice`, and unknown provider keys. Route direct `/v1/messages` JSON and streaming execution through this dedicated builder instead of `_execute_generic_once()` / `_execute_generic_stream_once()` when request-side sanitize is needed. Reuse the existing request pipeline, request context, and `/v1/responses` rewrite pattern; do not refactor generic provider execution or alter the compat conversion path.</action>
  <acceptance_criteria>
    `rg -n "_build_messages_upstream_payload|messages.*sanitize|generic_request_sanitize_unsupported" aegisgate/adapters/openai_compat/router.py aegisgate/adapters/openai_compat/sanitize.py`
    `pytest -q aegisgate/tests/test_request_redaction_routes.py::test_messages_request_redaction_preserves_anthropic_shape -x`
    `pytest -q aegisgate/tests/test_request_redaction_routes.py::test_messages_stream_request_redaction_preserves_anthropic_shape -x`
    `pytest -q aegisgate/tests/test_passthrough_filter_mode.py -k "messages_passthrough or messages_compat" -x`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_openai_request_mapping.py aegisgate/tests/test_passthrough_filter_mode.py -k "messages_request_redaction or anthropic_shape or messages_passthrough or messages_compat" -x</automated>
  </verify>
  <done>Direct `/v1/messages` now forwards redacted payloads in valid Anthropic shape for JSON and streaming, while compat mode still reuses `/v1/responses`.</done>
</task>

</tasks>

<verification>
Run the direct `/v1/messages` regression slice and confirm the old generic sanitize-unsupported path is no longer the source of request-side redaction behavior for non-compat messages traffic.
</verification>

<success_criteria>
Equivalent sensitive content sent through `/v1/messages` now yields the same request-side redaction outcome as chat/responses without breaking Anthropic-compatible payload structure.
</success_criteria>

<output>
After completion, create `.planning/phases/01-request-redaction-precision/01-02-SUMMARY.md`
</output>
