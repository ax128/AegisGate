---
phase: 01-request-redaction-precision
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - aegisgate/adapters/openai_compat/sanitize.py
  - aegisgate/filters/redaction.py
  - aegisgate/policies/rules/security_filters.yaml
  - aegisgate/tests/test_request_redaction_routes.py
  - aegisgate/tests/test_sanitize_helpers.py
autonomous: true
requirements:
  - SAFE-04
must_haves:
  truths:
    - "Benign prompts on supported `/v1` routes avoid excessive false-positive redaction that makes the request unusable."
    - "Chat, responses, and direct messages use the same route-parity request redaction intent for equivalent content."
    - "Explicit secret-bearing fields still redact after the precision changes."
  artifacts:
    - path: "aegisgate/filters/redaction.py"
      provides: "Route-aware request redaction selection that favors low false positives on supported `/v1` routes."
    - path: "aegisgate/adapters/openai_compat/sanitize.py"
      provides: "Shared upstream rewrite redaction behavior aligned with the request filter."
    - path: "aegisgate/tests/test_request_redaction_routes.py"
      provides: "Benign route-parity regression coverage."
  key_links:
    - from: "aegisgate/filters/redaction.py"
      to: "aegisgate/adapters/openai_compat/sanitize.py"
      via: "shared relaxed-pattern intent for supported `/v1` request rewriting."
      pattern: "_is_.*route|RELAXED_PII"
    - from: "aegisgate/policies/rules/security_filters.yaml"
      to: "aegisgate/filters/redaction.py"
      via: "only minimal regex tuning if a focused benign-example test still fails."
      pattern: "hostname|address|field_value_patterns"
---

<objective>
Reduce false positives on the three supported `/v1` request routes without reopening the route-coverage gaps closed in the earlier plans.

Purpose: satisfy `SAFE-04` by aligning route-parity redaction behavior around explicit secrets and preserving usable benign prompts.
Output: focused benign-example regressions plus the smallest route-aware precision changes in filter/helper logic or YAML rules.
</objective>

<execution_context>
@/home/yz/.codex/get-shit-done/workflows/execute-plan.md
@/home/yz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-request-redaction-precision/01-RESEARCH.md
@.planning/phases/01-request-redaction-precision/01-01-SUMMARY.md
@.planning/phases/01-request-redaction-precision/01-02-SUMMARY.md
@aegisgate/filters/redaction.py
@aegisgate/adapters/openai_compat/sanitize.py
@aegisgate/policies/rules/security_filters.yaml
@aegisgate/tests/test_sanitize_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benign-example precision regressions across supported `/v1` routes</name>
  <files>aegisgate/tests/test_request_redaction_routes.py, aegisgate/tests/test_sanitize_helpers.py</files>
  <read_first>
    .planning/phases/01-request-redaction-precision/01-RESEARCH.md
    .planning/phases/01-request-redaction-precision/01-01-SUMMARY.md
    .planning/phases/01-request-redaction-precision/01-02-SUMMARY.md
    aegisgate/filters/redaction.py
    aegisgate/adapters/openai_compat/sanitize.py
    aegisgate/policies/rules/security_filters.yaml
    aegisgate/tests/test_request_redaction_routes.py
    aegisgate/tests/test_sanitize_helpers.py
  </read_first>
  <action>Add focused tests for `SAFE-04` before changing precision logic. Cover equivalent benign prompts on `/v1/chat/completions`, `/v1/responses`, and direct `/v1/messages`, with examples such as infrastructure hostnames, security-review snippets, and non-secret address-like strings that research identified as likely false positives. In the same files, add an explicit non-regression asserting those benign prompts are forwarded normally and do not trigger an unintended 403/block response on the supported `/v1` routes. Add counter-cases proving explicit secrets or API keys still redact. Keep assertions route-level and helper-level; do not broaden into response-side or `/v2` tests.</action>
  <acceptance_criteria>
    `rg -n "test_benign_examples_avoid_false_positives|test_benign_examples_do_not_trigger_403|test_explicit_secret_still_redacts" aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_sanitize_helpers.py`
    `pytest -q aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_sanitize_helpers.py -k "benign_examples or explicit_secret or trigger_403" -x`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_sanitize_helpers.py -k "benign_examples or explicit_secret or trigger_403" -x</automated>
  </verify>
  <done>Benign route-parity examples, no-unexpected-403 precision guards, and explicit-secret counter-cases are locked into automated regression coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align low-false-positive request redaction behavior across chat, responses, and messages</name>
  <files>aegisgate/filters/redaction.py, aegisgate/adapters/openai_compat/sanitize.py, aegisgate/policies/rules/security_filters.yaml</files>
  <read_first>
    .planning/phases/01-request-redaction-precision/01-RESEARCH.md
    .planning/phases/01-request-redaction-precision/01-01-SUMMARY.md
    .planning/phases/01-request-redaction-precision/01-02-SUMMARY.md
    aegisgate/filters/redaction.py
    aegisgate/adapters/openai_compat/sanitize.py
    aegisgate/policies/rules/security_filters.yaml
    aegisgate/tests/test_request_redaction_routes.py
    aegisgate/tests/test_sanitize_helpers.py
  </read_first>
  <behavior>
    - Supported `/v1` routes share a low-false-positive request redaction intent for equivalent content.
    - Explicit secret-bearing fields, bearer tokens, and private-key material still redact.
    - No new request-sanitizer block paths or generic-executor rewrites are introduced in this precision pass.
  </behavior>
  <action>Implement the smallest route-aware precision change needed to satisfy the new benign-example tests. Prefer code changes in `aegisgate/filters/redaction.py` and `aegisgate/adapters/openai_compat/sanitize.py` that reuse the existing relaxed pattern subset currently reserved for `/v1/responses`, extending that intent to `/v1/chat/completions` and `/v1/messages` per the research guidance. Only touch `aegisgate/policies/rules/security_filters.yaml` if a specific focused benign-example test still fails after the route-parity logic is aligned; keep any YAML edits narrow and traceable to that test. Do not widen scope into response sanitization, `/v2`, or a general regex rewrite.</action>
  <acceptance_criteria>
    `rg -n "RELAXED_PII|_is_.*route|hostname|address" aegisgate/filters/redaction.py aegisgate/adapters/openai_compat/sanitize.py aegisgate/policies/rules/security_filters.yaml`
    `pytest -q aegisgate/tests/test_request_redaction_routes.py::test_benign_examples_avoid_false_positives -x`
    `pytest -q aegisgate/tests/test_request_redaction_routes.py::test_benign_examples_do_not_trigger_403 -x`
    `pytest -q aegisgate/tests/test_sanitize_helpers.py::test_explicit_secret_still_redacts -x`
    `pytest -q aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_openai_request_mapping.py aegisgate/tests/test_sanitize_helpers.py aegisgate/tests/test_passthrough_filter_mode.py -x`
  </acceptance_criteria>
  <verify>
    <automated>pytest -q aegisgate/tests/test_request_redaction_routes.py aegisgate/tests/test_openai_request_mapping.py aegisgate/tests/test_sanitize_helpers.py aegisgate/tests/test_passthrough_filter_mode.py -x</automated>
  </verify>
  <done>Supported `/v1` routes preserve benign prompt usability while still redacting explicit secrets, and the focused phase regression suite is green.</done>
</task>

</tasks>

<verification>
Run the full Phase 1 focused suite, then confirm the same benign example stays usable across chat, responses, and direct messages without unexpected 403 responses while explicit secrets still redact on all three routes.
</verification>

<success_criteria>
Normal prompts sent through any supported `/v1` route remain usable, explicit secrets still redact, and the phase-specific regression slice passes end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/01-request-redaction-precision/01-03-SUMMARY.md`
</output>
